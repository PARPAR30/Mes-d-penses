[package]
name = "mon-budget"
version = "1.0.0"
description = "Budget et dépenses par enveloppes — 100% local"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
